import React from "react";
import { AddUserForm, Layout } from "../components";

const AddUser = () => {
  return (
    <Layout title="Add a new User">
      <AddUserForm />
    </Layout>
  );
};

export default AddUser;
